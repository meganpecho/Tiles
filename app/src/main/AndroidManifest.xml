<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.androidclass.mepec_000.tiles">

    <application
        android:allowBackup="true"
        android:icon="@drawable/tiles_icon"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainMenu">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".GameModes" />
        <activity android:name=".Timed_Easy" />
        <activity android:name=".Timed_Medium" />
        <activity android:name=".SettingsActivity" />
        <activity android:name=".AboutActivity" />
        <activity android:name=".PopUpMenu"/>
        <activity android:name=".EndGameMenu"></activity>
    </application>

</manifest>